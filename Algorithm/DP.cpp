/**
 * TODO:
 */
#include <iostream>
#include <algorithm>

using namespace std;

static const int INF = (1 << 29);

int main()
{
    int m, n;
    cin >> m >> n;
    int c[m];
    for (size_t i = 1; i < m; i++)
    {
        cin >> c[m];
    }
    for (size_t i = 0; i < m; i++)
    {
    }

    sort(c, c + m);
    return 0;
}